const $ = s => document.querySelector(s), app = $('#app');
let session = JSON.parse(localStorage.getItem('cp_session') || 'null'), state = {}, page = 'overview';
const nav = {
 student:[['overview','🏠','Overview'],['agent','🧠','My AI Agent'],['performance','📊','Performance'],['subjects','📚','Subjects'],['attendance','📅','Attendance'],['assignments','📝','Assignments'],['assistant','🤖','AI Assistant'],['notifications','🔔','Notifications'],['settings','⚙','Settings']],
 teacher:[['overview','🏠','Overview'],['agent','🧠','Teacher AI Agent'],['students','👨‍🎓','My Students'],['search','🪪','Student ID Search'],['assignments','📝','Assignments'],['create','➕','Create Assignment'],['attendance','📅','Attendance'],['analytics','📈','Class Analytics'],['assistant','🤖','AI Insights'],['reports','📄','Reports'],['notifications','🔔','Notifications'],['settings','⚙','Settings']],
 admin:[['overview','🏠','Overview'],['students','👨‍🎓','Students'],['faculty','👨‍🏫','Faculty'],['analytics','📊','Analytics'],['agent','🤖','AI Insights'],['risk','🚨','Risk Center'],['reports','📄','Reports'],['settings','⚙','Settings']]
};
function api(url, options={}) { const headers={'Content-Type':'application/json',...(options.headers||{})}; if(session?.token) headers.Authorization=session.token; return fetch('/api'+url,{...options,headers}).then(async r=>{const d=await (r.status===204?Promise.resolve({}):r.json()); if(!r.ok) throw Error(d.message||'Request failed'); return d;}); }
function esc(s=''){return String(s).replace(/[&<>'"]/g,x=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[x]))}
function status(score){return score>=90?['EXCELLENT','good']:score>=75?['GOOD','good']:score>=60?['TO IMPROVE','warn']:['MORE TO IMPROVE','bad']}
function toast(message){$('#toast-root').innerHTML=`<div class="toast">${esc(message)}</div>`;setTimeout(()=>$('#toast-root').innerHTML='',3200)}
function modal(html){$('#modal-root').innerHTML=`<div class="modal-cover" onclick="if(event.target===this) closeModal()"><div class="modal">${html}</div></div>`} function closeModal(){$('#modal-root').innerHTML=''}
function applyTheme(){document.body.classList.toggle('dark',localStorage.getItem('cp_theme')==='dark')}
function brand(){return `<div class="brand">🎓 <span>COLLEGE PORTAL<small>Smart Academic Management</small></span></div>`}
function landing(){app.innerHTML=`<main class="landing"><header class="topbar">${brand()}<button class="theme" onclick="toggleTheme()">${document.body.classList.contains('dark')?'☀ Light':'🌙 Dark'}</button></header><section class="hero"><div><span class="eyebrow">Powered by EduInsight AI</span><h1>Smart academic<br><span>management.</span></h1><p>One intelligent platform connecting students, teachers and administrators with clear academic signals and practical next steps.</p><div class="actions"><button class="primary" onclick="roles()">Get Started</button><button class="secondary" onclick="document.querySelector('.features').scrollIntoView({behavior:'smooth'})">Explore Features</button></div></div><aside class="hero-visual"><div class="mini-title">EduInsight AI <span class="badge good">● GOOD</span></div><div class="health-ring"></div><div class="mini-row"><span>Academic overview</span><b>86 / 100</b></div><div class="mini-row"><span>Priority focus</span><b>Java</b></div></aside></section><section class="features"><span class="eyebrow">An intelligence layer for campus</span><h2>Designed for every academic role.</h2><div class="feature-grid"><article class="feature"><i>🎓</i><h3>Student Intelligence</h3><p>Track performance and receive practical AI recommendations.</p></article><article class="feature"><i>👨‍🏫</i><h3>Teacher Intelligence</h3><p>Manage classes, assessments, attendance and student risk.</p></article><article class="feature"><i>👨‍💼</i><h3>Administrative Intelligence</h3><p>Monitor institution-wide academic performance.</p></article><article class="feature"><i>🤖</i><h3>EduInsight AI</h3><p>Convert academic data into meaningful insights.</p></article></div></section></main>`}
function roles(){app.innerHTML=`<main class="auth-page"><section class="auth-box"><button class="back" onclick="landing()">← Back to home</button><div class="auth-head"><span class="eyebrow">Welcome to College Portal</span><h1>Select your role</h1><p class="muted">Choose the workspace that fits how you support learning.</p></div><div class="role-grid">${[['student','🎓','STUDENT','Personal academic intelligence.'],['teacher','👨‍🏫','TEACHER','Classroom management and AI analytics.'],['admin','👨‍💼','ADMIN','Institutional intelligence and management.']].map(x=>`<article class="role-card"><i>${x[1]}</i><h3>${x[2]}</h3><p>${x[3]}</p><button class="primary" onclick="login('${x[0]}')">Continue</button></article>`).join('')}</div></section></main>`}
function login(role){const labels={student:['Student','STU001'],teacher:['Faculty','FAC001'],admin:['Admin','ADM001']}[role];app.innerHTML=`<main class="auth-page"><section class="auth-box"><div class="login-top"><button class="back" onclick="roles()">← Back</button></div><div class="auth-head"><span class="eyebrow">${role==='admin'?'Secure ':' '}Access</span><h1>${labels[0]} login</h1></div><form class="login-form" onsubmit="doLogin(event,'${role}')"><div class="field"><label>${labels[0]} ID</label><input name="id" required placeholder="${labels[1]}"></div><div class="field"><label>Password</label><input name="password" type="password" required placeholder="••••"></div><div class="demo">Demo credentials: <b>${labels[1]}</b> / <b>1234</b></div><button class="primary" style="width:100%">${role==='admin'?'Secure Login':'Login'}</button></form></section></main>`}
async function doLogin(e,role){e.preventDefault();const f=new FormData(e.target);try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({role,id:f.get('id'),password:f.get('password')})});session=d;localStorage.setItem('cp_session',JSON.stringify(session));page='overview';await load();}catch(err){toast(err.message)}}
async function doLogin(e,role){e.preventDefault();const f=new FormData(e.target);try{const d=await api('/auth/login',{method:'POST',body:JSON.stringify({role,id:f.get('id'),password:f.get('password')})});session=d;localStorage.setItem('cp_session',JSON.stringify(session));page='overview';await load();}catch(err){toast(err.message)}}
async function load(){try{state=await api(`/dashboard/${session.user.role}`);shell()}catch(e){session=null;localStorage.removeItem('cp_session');landing();toast(e.message)}}
function shell(){const role=session.user.role;app.innerHTML=`<div class="shell"><aside class="sidebar">${brand()}<nav class="nav">${nav[role].map(x=>`<button class="${page===x[0]?'active':''}" onclick="go('${x[0]}')">${x[1]} <span>${x[2]}</span></button>`).join('')}<button class="logout" onclick="logout()">🚪 <span>Logout</span></button></nav></aside><main class="main"><header class="app-header"><h1>${nav[role].find(x=>x[0]===page)?.[2]||'College Portal'}</h1><div class="header-actions"><button class="theme" onclick="toggleTheme()">${document.body.classList.contains('dark')?'☀':'🌙'}</button><div class="profile-dot">${session.user.name[0]}</div></div></header><section class="content">${view()}</section></main></div>`}
function go(next){page=next;shell()} function logout(){session=null;localStorage.removeItem('cp_session');landing();toast('You have been signed out.')} function toggleTheme(){localStorage.setItem('cp_theme',document.body.classList.contains('dark')?'light':'dark');applyTheme(); if(session)shell();else landing()}
function pageHead(title,sub,action=''){return `<div class="page-head"><div><span class="eyebrow">Powered by EduInsight AI</span><h2>${title}</h2><p>${sub}</p></div>${action}</div>`}
function metric(label,value,delta='Live academic data'){return `<article class="card metric"><div class="label">${label}</div><div class="value">${value}</div><div class="delta">${delta}</div></article>`}
function chart(items){return `<div class="bar-chart">${items.map(x=>`<div class="bar" style="--h:${x.value}%"><em>${x.value}%</em><span>${x.label}</span></div>`).join('')}</div>`}
function student(){return state.student} function evaluated(s=student()){return state.submissions?.filter(x=>x.studentId===s.id&&typeof x.score==='number')||[]}
function view(){let r=session.user.role;return r==='student'?studentView():r==='teacher'?teacherView():adminView()}
function studentView(){const s=student(), p=page;if(p==='overview')return studentOverview(s);if(p==='agent')return studentAgent(s);if(p==='performance')return performance(s);if(p==='subjects')return subjects(s);if(p==='attendance')return attendance(s);if(p==='assignments')return assignments(s);if(p==='assistant')return chat('student');if(p==='notifications')return notifications();return settings()}
function studentOverview(s){return `${pageHead(`Good Morning, ${s.name} 👋`,`Here’s your academic overview.`)}<div class="card-grid">${metric('CGPA',s.cgpa,'Current semester')}${metric('Attendance',s.attendance+'%','Overall attendance')}${metric('Academic Score',s.academicScore+'%','Subject average')}${metric('AI Academic Health',s.academicHealth+'/100','Illustrative score')}</div><div class="two-col"><article class="panel"><div class="panel-header"><div><h3>Performance by subject</h3><span class="muted">Current academic score</span></div><span class="badge neutral">Semester 3</span></div>${chart(s.subjects.map(x=>({label:x.name.replace('Artificial ','AI'),value:x.marks})))}</article><article class="panel ai-panel"><span class="eyebrow" style="color:#bfdbfe">EDUINSIGHT AI</span><h3>Academic summary</h3><div class="ai-score">${s.academicHealth}<small>/100</small></div><span class="badge good">● ${status(s.academicHealth)[0]}</span><p>${review(s)}</p><button class="small-btn" onclick="go('agent')">Open my AI agent →</button></article></div><div class="two-col"><article class="panel"><div class="panel-header"><h3>Upcoming assignments</h3><button class="small-btn" onclick="go('assignments')">View all</button></div>${state.assignments.slice(0,3).map(a=>assignmentRow(a,s)).join('')||'<p class="muted">No assignments due.</p>'}</article><article class="panel"><h3>Recent scores</h3>${evaluated().map(x=>`<div class="list-row"><span>${assignmentBy(x.assignmentId)?.title||'Assignment'}</span><b>${x.score}/${x.maxScore}</b></div>`).join('')||'<p class="muted">Scores will appear after evaluation.</p>'}</article></div>`}
function review(s){const weak=s.subjects.reduce((a,x)=>a.marks<x.marks?a:x), strong=s.subjects.reduce((a,x)=>a.marks>x.marks?a:x);return `Your overall performance is ${s.academicHealth>=75?'good and improving':'developing'}. ${strong.name} is your strongest subject. ${weak.name} requires additional attention because ${weak.attendance<75?'attendance and ':''}assessment scores are below the recommended level.`}
function studentAgent(s){const st=status(s.academicHealth);return `${pageHead('EduInsight AI','Your personal academic agent analyzes grades, attendance, assignments and trends.')}<div class="two-col"><article class="panel ai-panel"><span class="eyebrow" style="color:#bfdbfe">PERSONAL ACADEMIC AGENT</span><div class="ai-score">${s.academicHealth}<small>/100</small></div><span class="badge ${st[1]}">● ${st[0]}</span><p>${review(s)}</p><p class="muted" style="color:#bfdbfe;font-size:.75rem">Illustrative scoring model: grades 40%, attendance 20%, assignments 20%, internal marks 10%, trend 10%.</p></article><article class="panel"><h3>Priority list</h3>${s.subjects.slice().sort((a,b)=>a.marks-b.marks).map(x=>`<div class="list-row"><span><b>${x.name}</b><br><small class="muted">${x.marks}% score · ${x.attendance}% attendance</small></span><span class="badge ${status(x.marks)[1]}">${status(x.marks)[0]}</span></div>`).join('')}</article></div><article class="panel" style="margin-top:18px"><h3>Recommended actions</h3><div class="quick-actions">${recommendations(s).map(x=>`<span class="small-btn">→ ${x}</span>`).join('')}</div></article>`}
function recommendations(s){const w=s.subjects.reduce((a,x)=>a.marks<x.marks?a:x);return [`Attend upcoming ${w.name} classes`,`Revise core ${w.name} concepts`,`Complete pending assignments`,`Schedule three focused practice sessions` ]}
function performance(s){return `${pageHead('Academic Performance','A subject-level view of your learning progress.')}<div class="two-col"><article class="panel"><h3>Performance chart</h3>${chart(s.subjects.map(x=>({label:x.name.replace('Artificial ','AI'),value:x.marks})))}</article><article class="panel"><h3>Score distribution</h3>${s.subjects.map(x=>`<div class="list-row"><span>${x.name}</span><span><b>${x.marks}%</b><div class="progress"><i style="--p:${x.marks}%"></i></div></span></div>`).join('')}</article></div>`}
function subjects(s){return `${pageHead('Subject-wise AI Analysis','Review scores, class participation and personalized recommendations.')}<div class="subject-grid">${s.subjects.map(x=>{let q=status(x.marks);return `<article class="card subject"><div class="panel-header"><h3>${x.name}</h3><span class="badge ${q[1]}">● ${q[0]}</span></div><div class="numbers"><div><b>${x.marks}%</b><span>Score</span></div><div><b>${x.attendance}%</b><span>Attendance</span></div><div><b>${x.assignments}%</b><span>Assignments</span></div></div><p>${x.marks<60?'Your performance needs immediate attention.':x.marks<75?'Your performance is acceptable, with room for improvement.':'Your performance is strong and consistent.'}</p><div class="recommend">→ ${x.attendance<75?'Attend upcoming classes and practice core concepts.':'Maintain your current study rhythm and revise weekly.'}</div></article>`}).join('')}</div>`}
function attendance(s){return `${pageHead('Attendance','Current attendance and projected improvement opportunities.')}<div class="card-grid">${metric('Overall Attendance',s.attendance+'%','52 present · 8 absent · 60 total')}${metric('Present','52','Recorded classes')}${metric('Absent','8','Recorded classes')}${metric('Classes','60','Current semester')}</div><div class="two-col"><article class="panel"><h3>Subject-wise attendance</h3>${s.subjects.map(x=>`<div class="list-row"><span>${x.name}</span><span><b>${x.attendance}%</b> <span class="badge ${x.attendance>=90?'good':x.attendance>=75?'warn':'bad'}">●</span></span></div>`).join('')}</article><article class="panel ai-panel"><span class="eyebrow" style="color:#bfdbfe">ATTENDANCE PREDICTION</span><h3>Java improvement plan</h3><p>If you attend the next 5 Java classes, your attendance could reach approximately <b>${Math.round((Math.round(s.subjects.find(x=>x.name==='Java').attendance*.6)+5)/(Math.round(.6)+5)*100)||78}%</b>.</p><button class="small-btn" onclick="toast('Prediction uses the illustrative attendance formula.')">How it’s calculated</button></article></div>`}
function assignmentBy(id){return state.assignments.find(x=>x.id===id)} function assignmentRow(a,s){const sub=(state.submissions||[]).find(x=>x.assignmentId===a.id&&x.studentId===s.id),due=new Date(a.dueDate)<new Date();return `<div class="assignment card"><div class="panel-header"><div><h3>${esc(a.title)}</h3><div class="meta"><span>${a.subject}</span><span>Due: ${a.dueDate}</span><span>Max: ${a.maxScore}</span></div></div><span class="badge ${sub?'good':due?'bad':'warn'}">${sub?(typeof sub.score==='number'?`Scored ${sub.score}/${a.maxScore}`:'Submitted'):(due?'Overdue':'Pending')}</span></div><div class="assignment-actions"><button class="small-btn" onclick="viewAssignment('${a.id}')">View assignment</button>${!sub?`<button class="primary" onclick="submitAssignment('${a.id}')">Submit</button>`:''}</div></div>`}
function assignments(s){return `${pageHead('My Assignments','Submit coursework and follow evaluation status.')}<div class="tabs"><button class="active" onclick="filterAssignments('all',this)">All</button><button onclick="filterAssignments('pending',this)">Pending</button><button onclick="filterAssignments('submitted',this)">Submitted</button></div><div id="assignment-list">${state.assignments.map(a=>assignmentRow(a,s)).join('')||'<div class="empty">No assignments yet.</div>'}</div>`}
function filterAssignments(filter,el){document.querySelectorAll('.tabs button').forEach(x=>x.classList.remove('active'));el.classList.add('active');const s=student();let a=state.assignments;if(filter==='pending')a=a.filter(x=>!state.submissions.some(q=>q.assignmentId===x.id&&q.studentId===s.id));if(filter==='submitted')a=a.filter(x=>state.submissions.some(q=>q.assignmentId===x.id&&q.studentId===s.id));$('#assignment-list').innerHTML=a.map(x=>assignmentRow(x,s)).join('')||'<div class="empty">No matching assignments.</div>'}
function viewAssignment(id){let a=assignmentBy(id);modal(`<button class="close" onclick="closeModal()">×</button><h2>${esc(a.title)}</h2><p class="muted">${esc(a.description||'No description provided.')}</p><div class="list-row"><span>Subject</span><b>${a.subject}</b></div><div class="list-row"><span>Due date</span><b>${a.dueDate}</b></div><div class="list-row"><span>Maximum score</span><b>${a.maxScore}</b></div>`)}
function submitAssignment(id){let a=assignmentBy(id);modal(`<button class="close" onclick="closeModal()">×</button><h2>Assignment Submission</h2><p><b>Assignment:</b> ${esc(a.title)}</p><form onsubmit="saveSubmission(event,'${id}')"><div class="field"><label>Upload (simulated)</label><input name="file" type="file"></div><div class="field"><label>Comment</label><textarea name="comment" rows="4" placeholder="Add a note for your teacher"></textarea></div><button class="primary">Submit Assignment</button></form>`)}
async function saveSubmission(e,id){e.preventDefault();let f=new FormData(e.target);try{await api(`/assignments/${id}/submit`,{method:'POST',body:JSON.stringify({comment:f.get('comment'),fileName:f.get('file').name})});closeModal();toast('Assignment submitted successfully.');await load()}catch(x){toast(x.message)}}
function chat(role){const prompts=role==='student'?['Why is my Java performance low?','Which subject should I study?','Analyze my attendance.','Create a study plan.']:['Which students are at risk?','Which subject has the lowest average?','Who has low attendance?','Who needs additional assignments?'];return `${pageHead(role==='student'?'EduInsight AI Assistant':'Teacher AI Insights','Ask questions grounded in the current demo academic data.')}<article class="panel chat"><div class="quick-actions">${prompts.map(x=>`<button class="small-btn" onclick="ask('${esc(x)}')">${x}</button>`).join('')}</div><div class="messages" id="messages"><div class="msg">Hello, ${session.user.name}. Ask me anything about the available academic data.</div></div><form onsubmit="sendChat(event)"><input name="question" required placeholder="Ask EduInsight AI…"><button class="primary">Send</button></form></article>`}
async function ask(q){$('#messages').innerHTML+=`<div class="msg user">${q}</div>`;try{let d=await api('/ai/chat',{method:'POST',body:JSON.stringify({question:q})});$('#messages').innerHTML+=`<div class="msg">${esc(d.answer)}</div>`}catch(e){toast(e.message)}}function sendChat(e){e.preventDefault();let q=new FormData(e.target).get('question');e.target.reset();ask(q)}
function notifications(){setTimeout(loadNotifications,0);return `${pageHead('Notifications','Updates generated by classroom activity and EduInsight AI.')}<article class="panel" id="note-list"><p class="muted">Loading notifications…</p></article>`}async function loadNotifications(){try{const notes=await api('/notifications');$('#note-list').innerHTML=notes.length?notes.map(n=>`<div class="list-row"><span>${esc(n.text)}</span><small class="muted">${n.date.slice(0,10)}</small></div>`).join(''):'<div class="empty">You are all caught up.</div>'}catch(e){toast(e.message)}}
function settings(){return `${pageHead('Settings','Personalize your College Portal workspace.')}<article class="panel form-card"><h3>Appearance</h3><p class="muted">Choose a theme; your preference is saved in this browser.</p><button class="primary" onclick="toggleTheme()">${document.body.classList.contains('dark')?'☀ Switch to Light':'🌙 Switch to Dark'}</button></article>`}
function teacherView(){if(page==='overview')return teacherOverview();if(page==='agent')return teacherAgent();if(page==='students')return teacherStudents();if(page==='search')return studentSearch();if(page==='assignments')return teacherAssignments();if(page==='create')return createAssignment();if(page==='attendance')return teacherAttendance();if(page==='analytics')return teacherAnalytics();if(page==='assistant')return chat('teacher');if(page==='reports')return reports();if(page==='notifications')return notifications();return settings()}
function classStats(){const students=state.students, avg=Math.round(students.reduce((a,x)=>a+x.academicScore,0)/students.length),att=Math.round(students.reduce((a,x)=>a+x.attendance,0)/students.length),risk=students.filter(x=>x.risk==='HIGH RISK').length,attention=students.filter(x=>x.risk==='NEEDS ATTENTION').length;return {students,avg,att,risk,attention,health:Math.round(avg*.7+att*.3)}}
function teacherOverview(){const x=classStats(),pending=state.submissions.filter(s=>s.status==='submitted'&&typeof s.score!=='number').length;return `${pageHead(`Good Morning, ${state.teacher.name} 👋`,`Here’s your classroom intelligence.`)}<div class="card-grid">${metric('Students',x.students.length,'Assigned class')}${metric('Classes',state.teacher.classes.length,'Active classes')}${metric('Average Score',x.avg+'%','Class average')}${metric('Attendance',x.att+'%','Class average')}</div><div class="card-grid">${metric('🔴 At-Risk Students',x.risk,'Requires intervention')}${metric('🟡 Needs Attention',x.attention,'Monitor closely')}${metric('🟢 Stable',x.students.length-x.risk-x.attention,'On track')}${metric('📝 Pending Evaluations',pending,'Submitted work')}</div><div class="two-col"><article class="panel"><div class="panel-header"><h3>Today’s assignments</h3><button class="small-btn" onclick="go('assignments')">Manage</button></div>${state.assignments.map(a=>`<div class="list-row"><span><b>${esc(a.title)}</b><br><small class="muted">${a.subject} · Due ${a.dueDate}</small></span><span>${state.submissions.filter(s=>s.assignmentId===a.id).length}/${x.students.length} <small class="muted">submissions</small></span></div>`).join('')||'<p class="muted">No assignments created.</p>'}</article><article class="panel ai-panel"><span class="eyebrow" style="color:#bfdbfe">TEACHER AI AGENT</span><div class="ai-score">${x.health}<small>/100</small></div><span class="badge good">● GOOD</span><p>Your class is performing well overall. ${weakSubject(x.students).name} has the lowest class average and ${x.risk} student(s) require academic attention.</p><button class="small-btn" onclick="go('agent')">Open AI insight →</button></article></div>`}
function weakSubject(students){let all={};students.forEach(s=>s.subjects.forEach(x=>(all[x.name]??=[]).push(x.marks)));return Object.entries(all).map(([name,v])=>({name,value:Math.round(v.reduce((a,b)=>a+b,0)/v.length)})).sort((a,b)=>a.value-b.value)[0]}
function teacherAgent(){const x=classStats(),weak=weakSubject(x.students);return `${pageHead('Teacher AI Agent','Your classroom academic intelligence assistant.')}<div class="two-col"><article class="panel ai-panel"><span class="eyebrow" style="color:#bfdbfe">CLASS HEALTH</span><div class="ai-score">${x.health}<small>/100</small></div><span class="badge good">● GOOD</span><p>Your class is performing well overall. ${weak.name} has the lowest class average at ${weak.value}%.</p></article><article class="panel"><h3>Generated insights</h3><div class="list"><div class="list-row"><span>📊 Class performance</span><b>${x.avg}% average</b></div><div class="list-row"><span>📉 Weakest subject</span><b>${weak.name} · ${weak.value}%</b></div><div class="list-row"><span>📅 Attendance alert</span><b>${x.students.filter(s=>s.attendance<75).length} below 75%</b></div><div class="list-row"><span>🔴 Academic risk</span><b>${x.risk} high risk</b></div></div></article></div><article class="panel" style="margin-top:18px"><h3>Recommended actions</h3><div class="quick-actions"><span class="small-btn">→ Conduct ${weak.name} revision session</span><span class="small-btn">→ Contact at-risk students</span><span class="small-btn">→ Create additional practice</span><span class="small-btn">→ Monitor low attendance</span></div></article>`}
function teacherStudents(){return `${pageHead('My Students','Students assigned to your current classes.')}<article class="panel table-wrap"><table class="table"><thead><tr><th>Student</th><th>Class</th><th>Academic Score</th><th>Attendance</th><th>Risk</th><th></th></tr></thead><tbody>${state.students.map(s=>`<tr><td><b>${s.name}</b><br><small class="muted">${s.id}</small></td><td>${s.className}</td><td>${s.academicScore}%</td><td>${s.attendance}%</td><td><span class="badge ${s.risk==='STABLE'?'good':s.risk==='NEEDS ATTENTION'?'warn':'bad'}">${s.risk}</span></td><td><button class="small-btn" onclick="openStudent('${s.id}')">Report</button></td></tr>`).join('')}</tbody></table></article>`}
function studentSearch(){return `${pageHead('Student ID Search','Search only the students assigned to your classes.')}<article class="panel"><form class="search-row" onsubmit="findStudent(event)"><input required name="id" placeholder="Enter Student ID (e.g. STU001)"><button class="primary">Search</button></form><div id="search-result" class="empty">Enter a Student ID to view their academic profile.</div></article>`}
async function findStudent(e){e.preventDefault();let id=new FormData(e.target).get('id').trim().toUpperCase();try{let d=await api('/students/'+id);renderSearch(d.student)}catch(x){$('#search-result').innerHTML=`<p class="bad">${esc(x.message)}</p>`}}
function renderSearch(s){$('#search-result').innerHTML=`<div class="panel" style="text-align:left;margin-top:20px"><div class="panel-header"><h3>Student Profile</h3><span class="badge ${s.risk==='STABLE'?'good':s.risk==='NEEDS ATTENTION'?'warn':'bad'}">${s.risk}</span></div><div class="split"><div><div class="list-row"><span>Name</span><b>${s.name}</b></div><div class="list-row"><span>Student ID</span><b>${s.id}</b></div><div class="list-row"><span>Department</span><b>${s.department}</b></div></div><div><div class="list-row"><span>CGPA</span><b>${s.cgpa}</b></div><div class="list-row"><span>Attendance</span><b>${s.attendance}%</b></div><div class="list-row"><span>Academic Score</span><b>${s.academicScore}%</b></div></div></div><button class="primary" style="margin-top:15px" onclick="openStudent('${s.id}')">View Full Report</button></div>`}
async function openStudent(id){try{let d=await api('/students/'+id);let s=d.student;modal(`<button class="close" onclick="closeModal()">×</button><h2>Student Report — ${s.name}</h2><p class="muted">${s.id} · ${s.department} · Year ${s.year}, Semester ${s.semester}</p><div class="card-grid"><div class="metric card"><div class="label">CGPA</div><div class="value">${s.cgpa}</div></div><div class="metric card"><div class="label">Attendance</div><div class="value">${s.attendance}%</div></div></div><h3>Subject Performance</h3>${s.subjects.map(x=>`<div class="list-row"><span>${x.name}</span><span>${x.marks}% · <small class="muted">${x.attendance}% attendance</small></span></div>`).join('')}<h3>AI Review</h3><p>${review(s)}</p><div class="recommend">Strength: ${s.subjects.reduce((a,x)=>a.marks>x.marks?a:x).name} · Needs attention: ${s.subjects.reduce((a,x)=>a.marks<x.marks?a:x).name}</div>`)}catch(e){toast(e.message)}}
function teacherAssignments(){return `${pageHead('Assignments','Create, evaluate and manage your coursework.',`<button class="primary" onclick="go('create')">+ Create Assignment</button>`)}<div class="list">${state.assignments.map(a=>teacherAssignment(a)).join('')||'<div class="empty">No assignments created.</div>'}</div>`}
function teacherAssignment(a){let count=state.submissions.filter(x=>x.assignmentId===a.id).length;return `<article class="assignment card"><div class="panel-header"><div><h3>${esc(a.title)}</h3><div class="meta"><span>${a.subject}</span><span>${a.className}</span><span>Due ${a.dueDate}</span><span>${count}/${state.students.length} submissions</span></div></div><span class="badge ${a.priority==='High'?'bad':'neutral'}">${a.priority||'Normal'}</span></div><div class="assignment-actions"><button class="small-btn" onclick="evaluate('${a.id}')">Evaluate</button><button class="small-btn" onclick="editAssignment('${a.id}')">Edit</button><button class="small-btn" onclick="deleteAssignment('${a.id}')">Delete</button></div></article>`}
function createAssignment(a={}){return `${pageHead(a.id?'Edit Assignment':'Create Assignment','Assignments are automatically visible to students in the selected class.')}<form class="panel form-card" onsubmit="saveAssignment(event,'${a.id||''}')"><div class="split"><div class="field"><label>Class</label><select name="className"><option>II-AIDS-A</option></select></div><div class="field"><label>Subject</label><select name="subject">${['Java','Data Structures','Python','Data Science'].map(x=>`<option ${a.subject===x?'selected':''}>${x}</option>`).join('')}</select></div></div><div class="field"><label>Assignment Title</label><input name="title" required value="${esc(a.title||'')}" placeholder="OOP Inheritance Practice"></div><div class="field"><label>Description</label><textarea name="description" rows="3">${esc(a.description||'')}</textarea></div><div class="split"><div class="field"><label>Due Date</label><input name="dueDate" type="date" required value="${a.dueDate||''}"></div><div class="field"><label>Maximum Score</label><input name="maxScore" type="number" min="1" required value="${a.maxScore||10}"></div></div><div class="field"><label>Priority</label><select name="priority"><option ${a.priority==='High'?'selected':''}>High</option><option ${a.priority==='Normal'?'selected':''}>Normal</option><option ${a.priority==='Low'?'selected':''}>Low</option></select></div><button class="primary">${a.id?'Save Changes':'Create Assignment'}</button></form>`}
function editAssignment(id){createAssignment(state.assignments.find(x=>x.id===id))}async function saveAssignment(e,id){e.preventDefault();let o=Object.fromEntries(new FormData(e.target));o.maxScore=Number(o.maxScore);try{await api(`/assignments${id?'/'+id:''}`,{method:id?'PUT':'POST',body:JSON.stringify(o)});toast(id?'Assignment updated.':'Assignment created and shared with students.');page='assignments';await load()}catch(x){toast(x.message)}}async function deleteAssignment(id){if(!confirm('Delete this assignment?'))return;try{await api('/assignments/'+id,{method:'DELETE'});toast('Assignment deleted.');await load()}catch(e){toast(e.message)}}
function evaluate(id){let a=state.assignments.find(x=>x.id===id), subs=state.submissions.filter(x=>x.assignmentId===id);modal(`<button class="close" onclick="closeModal()">×</button><h2>${esc(a.title)}</h2><p class="muted">Maximum Score: ${a.maxScore}</p>${subs.length?subs.map(s=>{let stu=state.students.find(x=>x.id===s.studentId);return `<form class="panel" style="margin:12px 0" onsubmit="saveScore(event,'${id}','${s.studentId}')"><b>${stu?.name||s.studentId}</b> <small class="muted">${s.studentId} · Submitted ✓</small><p class="muted">${esc(s.comment||'No comment')}</p><div class="split"><div class="field"><label>Score</label><input name="score" type="number" min="0" max="${a.maxScore}" required value="${typeof s.score==='number'?s.score:''}"></div><div class="field"><label>Feedback</label><input name="feedback" value="${esc(s.feedback||'')}"></div></div><button class="primary">Save Score</button></form>`}).join(''):'<div class="empty">No submissions yet. Students must submit before evaluation.</div>'}`)}
async function saveScore(e,id,studentId){e.preventDefault();let f=new FormData(e.target);try{let d=await api(`/assignments/${id}/evaluate/${studentId}`,{method:'POST',body:JSON.stringify({score:f.get('score'),feedback:f.get('feedback')})});toast(`Score saved successfully: ${d.submission.score}/${d.submission.maxScore}`);closeModal();await load()}catch(x){toast(x.message)}}
function teacherAttendance(){return `${pageHead('Attendance Management','Mark attendance; student attendance updates immediately.')}<article class="panel"><form onsubmit="saveAttendance(event)"><div class="split"><div class="field"><label>Class</label><select name="className"><option>II-AIDS-A</option></select></div><div class="field"><label>Subject</label><select name="subject">${['Java','Data Structures'].map(x=>`<option>${x}</option>`).join('')}</select></div></div><div class="table-wrap"><table class="table"><thead><tr><th>Student ID</th><th>Student</th><th>Present</th></tr></thead><tbody>${state.students.map(s=>`<tr><td>${s.id}</td><td>${s.name}</td><td><input type="checkbox" name="present" value="${s.id}" checked></td></tr>`).join('')}</tbody></table></div><div class="actions"><button type="button" class="secondary" onclick="document.querySelectorAll('[name=present]').forEach(x=>x.checked=true)">Mark All Present</button><button class="primary">Save Attendance</button></div></form></article>`}
async function saveAttendance(e){e.preventDefault();let f=new FormData(e.target), yes=f.getAll('present');try{await api('/attendance',{method:'POST',body:JSON.stringify({className:f.get('className'),subject:f.get('subject'),records:state.students.map(s=>({studentId:s.id,present:yes.includes(s.id)}))})});toast('Attendance saved successfully.');await load()}catch(x){toast(x.message)}}
function teacherAnalytics(){const x=classStats();return `${pageHead('Class Analytics','Live classroom performance and risk indicators.')}<div class="card-grid">${metric('Class Average',x.avg+'%')}${metric('Attendance',x.att+'%')}${metric('Assignment Completion',Math.round((state.submissions.length/Math.max(1,state.assignments.length*x.students.length))*100)+'%')}${metric('Academic Health',x.health+'/100')}</div><div class="two-col"><article class="panel"><h3>Student performance</h3>${chart(x.students.map(s=>({label:s.name,value:s.academicScore})))}</article><article class="panel"><h3>Subject averages</h3>${chart([...new Set(x.students.flatMap(s=>s.subjects.map(q=>q.name)))].map(n=>({label:n.replace('Artificial ','AI'),value:Math.round(x.students.reduce((a,s)=>a+s.subjects.find(q=>q.name===n).marks,0)/x.students.length)})))}</article></div>`}
function reports(){return `${pageHead('Reports','Generate classroom reports using current academic data.')}<article class="panel"><h3>Available reports</h3><div class="quick-actions">${['Individual Student Report','Class Performance Report','Attendance Report','Assignment Report','At-Risk Student Report','Subject Performance Report'].map(x=>`<button class="small-btn" onclick="previewReport('${x}')">${x}</button>`).join('')}</div></article>`}function previewReport(name){modal(`<button class="close" onclick="closeModal()">×</button><h2>${name}</h2><p>Generated from the current College Portal demo data on ${new Date().toLocaleDateString()}.</p><p>${session.user.role==='teacher'?`Class average: ${classStats().avg}% · Attendance: ${classStats().att}%`:'Institutional demonstration report.'}</p><button class="primary" onclick="window.print()">Print Report</button></div>`)}
function adminView(){if(page==='overview')return adminOverview();if(page==='students')return adminStudents();if(page==='faculty')return faculty();if(page==='analytics')return adminAnalytics();if(page==='agent')return adminAgent();if(page==='risk')return riskCenter();if(page==='reports')return reports();return settings()}
function adminOverview(){const ss=state.students,avg=Math.round(ss.reduce((a,x)=>a+x.academicScore,0)/ss.length),att=Math.round(ss.reduce((a,x)=>a+x.attendance,0)/ss.length);return `${pageHead('Institutional Intelligence','A live executive view of academic signals across the institution.')}<div class="card-grid">${metric('Students','5,482','Demo institutional total')}${metric('Faculty','312','Demo institutional total')}${metric('Departments','18','Academic departments')}${metric('Attendance','87.4%','Institutional benchmark')}</div><div class="two-col"><article class="panel"><h3>Department performance</h3>${chart([{label:'AI & DS',value:avg},{label:'CSE',value:84},{label:'ECE',value:79},{label:'ME',value:72}])}</article><article class="panel ai-panel"><span class="eyebrow" style="color:#bfdbfe">EDUINSIGHT AI</span><h3>Institution highlights</h3><p>AI & DS performance increased by 8%. Java remains the lowest institutional subject average. ${ss.filter(x=>x.risk!=='STABLE').length} active demo students need intervention.</p><button class="small-btn" onclick="go('risk')">Open Risk Center →</button></article></div>`}
function adminStudents(){return `${pageHead('Students','Institution-wide student intelligence.')}<article class="panel table-wrap"><table class="table"><thead><tr><th>Student</th><th>Department</th><th>Score</th><th>Attendance</th><th>Risk</th></tr></thead><tbody>${state.students.map(s=>`<tr><td><b>${s.name}</b><br><small class="muted">${s.id}</small></td><td>${s.department}</td><td>${s.academicScore}%</td><td>${s.attendance}%</td><td><span class="badge ${s.risk==='STABLE'?'good':s.risk==='NEEDS ATTENTION'?'warn':'bad'}">${s.risk}</span></td></tr>`).join('')}</tbody></table></article>`}
function faculty(){return `${pageHead('Faculty','Faculty and allocated classroom intelligence.')}<article class="panel table-wrap"><table class="table"><thead><tr><th>Faculty</th><th>Department</th><th>Classes</th><th>Subjects</th></tr></thead><tbody>${state.teachers.map(t=>`<tr><td><b>${t.name}</b><br><small class="muted">${t.id}</small></td><td>${t.department}</td><td>${t.classes.join(', ')}</td><td>${t.subjects.join(', ')}</td></tr>`).join('')}</tbody></table></article>`}
function adminAnalytics(){const ss=state.students;return `${pageHead('Institutional Analytics','Department performance, attendance, pass rate and academic risk.')}<div class="card-grid">${metric('Pass Rate','91.2%')}${metric('Demo Score',Math.round(ss.reduce((a,x)=>a+x.academicScore,0)/ss.length)+'%')}${metric('Demo Attendance',Math.round(ss.reduce((a,x)=>a+x.attendance,0)/ss.length)+'%')}${metric('At Risk',ss.filter(x=>x.risk==='HIGH RISK').length)}</div><article class="panel"><h3>Academic score distribution</h3>${chart(ss.map(s=>({label:s.name,value:s.academicScore})))}</article>`}
function adminAgent(){return `${pageHead('EduInsight AI','Institutional academic intelligence and recommendation center.')}<article class="panel ai-panel"><span class="eyebrow" style="color:#bfdbfe">INSTITUTIONAL INSIGHT</span><h3>Recommended institutional action</h3><p>Prioritize Java support sessions and review low-attendance cohorts. These insights are generated from the current demonstration data and are not academic decisions.</p></article><article class="panel" style="margin-top:18px"><h3>Current AI insights</h3><div class="list"><div class="list-row"><span>AI & DS performance increased</span><b>+8%</b></div><div class="list-row"><span>Students requiring intervention</span><b>43</b></div><div class="list-row"><span>Attendance concerns</span><b>128</b></div><div class="list-row"><span>Lowest institution-wide average</span><b>Java</b></div></div></article>`}
function riskCenter(){const ss=state.students;return `${pageHead('Risk Center','Filterable academic intervention indicators.')}<div class="card-grid">${metric('🔴 Critical',ss.filter(s=>s.risk==='HIGH RISK').length)}${metric('🟠 Attention',ss.filter(s=>s.risk==='NEEDS ATTENTION').length)}${metric('🟢 Stable',ss.filter(s=>s.risk==='STABLE').length)}</div><article class="panel"><div class="panel-header"><h3>Student risk list</h3><select onchange="filterRisk(this.value)"><option value="all">All risk levels</option><option value="HIGH RISK">Critical</option><option value="NEEDS ATTENTION">Attention</option><option value="STABLE">Stable</option></select></div><div id="risk-list">${riskRows(ss)}</div></article>`}function riskRows(ss){return ss.map(s=>`<div class="list-row"><span><b>${s.name}</b><br><small class="muted">${s.department} · ${s.attendance}% attendance · ${s.academicScore}% score</small></span><span class="badge ${s.risk==='STABLE'?'good':s.risk==='NEEDS ATTENTION'?'warn':'bad'}">${s.risk}</span></div>`).join('')}function filterRisk(val){$('#risk-list').innerHTML=riskRows(val==='all'?state.students:state.students.filter(s=>s.risk===val))}
applyTheme(); if(session)load();else landing();
